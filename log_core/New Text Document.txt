       <div class="container my-4">

              <h2>Completed tasks by your members</h2>
              {% for task in task_list %}
              <b><p>{{task.name}}</p></b>
              <p>{{task.date}}</p>
              <b>Topic Covered</b> {{task.task_done}} <br>
             <b>Major: </b> {{task.course}}
              <hr>
              {% endfor %}
              
       </div>


# class team_lead(View):
     
#        def get(self,request):

#               groups = None
#               student_data = Groups.get_all_groups()
#               id_data = request.GET.get('id')

#               if id_data:
#                      groups = Student.get_groups_by_id(id_data)     
#               else:
#                      groups = Student.get_all_groups()

#               data = {}
#               data['groups'] = groups
#               data['student_data'] = student_data
              
#               return render(request, 'team_lead.html', data)